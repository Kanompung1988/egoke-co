# 🎁 คู่มือแจกแต้มโบนัสและ Migration ตั๋ว

## 📋 ภาพรวม

ระบบนี้ประกอบด้วย 3 สคริปหลัก:
1. **เช็คตั๋วทั้งหมด** - ตรวจสอบตั๋วในระบบ
2. **Migration ตั๋วเก่า** - ย้ายตั๋วจาก user history → tickets collection
3. **แจกแต้มโบนัส** - แจก 50 แต้มทุกคน + สร้าง notification

---

## 🚀 วิธีใช้งาน (รันตามลำดับ)

### ขั้นตอนที่ 1: เช็คตั๋วทั้งหมด

```bash
node check-all-tickets.cjs
```

**ผลลัพธ์ที่ได้:**
- จำนวนตั๋วใหม่ใน `tickets` collection
- จำนวนตั๋วเก่าใน `users/*/history`
- รายละเอียดตั๋วที่ต้อง migrate

---

### ขั้นตอนที่ 2: Migration ตั๋วเก่า (ถ้ามี)

```bash
node migrate-old-tickets.cjs
```

**สิ่งที่สคริปทำ:**
- ✅ ดึงตั๋วจาก `users/{userId}/history`
- ✅ ย้ายไปยัง `tickets` collection
- ✅ เก็บข้อมูล: ticketId, userId, userName, prize, claimed, timestamp
- ✅ ข้าม ticket ที่มีอยู่แล้ว
- ✅ เพิ่ม field: `migratedFrom: 'user-history'`, `migratedAt`

**ผลลัพธ์:**
```
📊 สรุปผลการ Migration:
   ✅ Migrate สำเร็จ: X ตั๋ว
   ⏭️  Skip (มีอยู่แล้ว): Y ตั๋ว
   ❌ Error: 0 ตั๋ว
```

---

### ขั้นตอนที่ 3: แจกแต้มโบนัส 🎉

```bash
node add-bonus-points.cjs
```

**สิ่งที่สคริปทำ:**
1. ✅ แจก **50 แต้ม** ให้ทุก user
2. ✅ อัปเดต user document:
   - `points: +50`
   - `lastBonusPoints: 50`
   - `lastBonusReason: "เฉลิมฉลอง..."`
   - `lastBonusAt: timestamp`
3. ✅ บันทึก activity log
4. ✅ สร้าง global notification

**ข้อความแจ้งเตือน:**
> 🎉 ได้รับแต้มโบนัส!
> 
> คุณได้รับแต้มโบนัส 50 แต้ม เนื่องจากเราต้องการเฉลิมฉลองและขอบคุณผู้เข้าร่วมงานทุกท่านที่ให้การตอบรับในงานวันแรกอย่างล้นหลาม! 🎊

---

## 🔔 ระบบแจ้งเตือนในเว็บ

### Component: `BonusNotification.tsx`

**คุณสมบัติ:**
- ✅ โชว์ป๊อปอัพสวยงามเมื่อ user login
- ✅ แสดง **แค่ครั้งเดียว** ต่อ user
- ✅ บันทึกว่าเห็นแล้วใน `users/{uid}/seenNotifications/bonus_points`
- ✅ ดึงข้อมูลจาก `notifications` collection (isGlobal: true)

**การติดตั้ง:**
- เพิ่มใน `Router.tsx` แล้ว (`<BonusNotification />`)
- จะทำงานอัตโนมัติเมื่อ user login

---

## 📊 โครงสร้าง Collections

### 1. `notifications`
```typescript
{
  type: 'bonus_points',
  title: '🎉 ได้รับแต้มโบนัส!',
  message: '...',
  points: 50,
  createdAt: Timestamp,
  showOnce: true,
  isGlobal: true
}
```

### 2. `users/{uid}/seenNotifications/bonus_points`
```typescript
{
  notificationId: string,
  seenAt: Timestamp,
  type: 'bonus_points'
}
```

### 3. `tickets` (หลัง migration)
```typescript
{
  ticketId: string,
  userId: string,
  userName: string,
  prize: string,
  emoji: string,
  claimed: boolean,
  timestamp: Timestamp,
  migratedFrom?: 'user-history',  // ถ้าเป็นตั๋วเก่า
  migratedAt?: Timestamp
}
```

---

## ⚠️ ข้อควรระวัง

### 🔴 รันแค่ครั้งเดียว!
- ❌ **อย่ารัน `add-bonus-points.cjs` ซ้ำ** - จะแจกแต้มซ้ำ!
- ✅ ถ้ารันพลาด ต้องลบ notification และลบ seenNotifications ก่อน

### 🟡 Migration ปลอดภัย
- ✅ สามารถรันซ้ำได้ - จะข้าม ticket ที่มีแล้ว
- ✅ ไม่ลบ ticket เก่าใน user history

### 🟢 Firestore Rules
ตรวจสอบว่ามี rule สำหรับ `notifications` collection:

```javascript
// firestore.rules
match /notifications/{notificationId} {
  allow read: if request.auth != null;
  allow write: if false; // สร้างผ่าน script เท่านั้น
}

match /users/{userId}/seenNotifications/{notifId} {
  allow read, write: if request.auth != null && request.auth.uid == userId;
}
```

---

## 🎯 ขั้นตอนการ Deploy

### 1. Deploy Code
```bash
# Build
npm run build

# Deploy to Vercel
vercel deploy --prod
```

### 2. รัน Scripts (ในเครื่อง)
```bash
# 1. เช็คก่อน
node check-all-tickets.cjs

# 2. Migrate ตั๋วเก่า (ถ้ามี)
node migrate-old-tickets.cjs

# 3. แจกแต้ม (รันครั้งเดียว!)
node add-bonus-points.cjs
```

### 3. Deploy Firestore Rules (ถ้ายังไม่ได้ทำ)
```bash
firebase deploy --only firestore:rules
```

---

## 📈 ตัวอย่างผลลัพธ์

### เช็คตั๋ว
```
📊 1. ตั๋วใน tickets collection (ระบบใหม่):
   ✅ พบตั๋วใหม่: 25 ตั๋ว
   📈 สถิติตั๋วใหม่:
      - ยังไม่เคลม: 18 ตั๋ว
      - เคลมแล้ว: 7 ตั๋ว

📊 2. ตั๋วเก่าใน users/*/history (ก่อนอัปเดต):
   ✅ พบผู้ใช้ที่มีตั๋วเก่า: 12 คน
   ✅ พบตั๋วเก่า: 35 ตั๋ว
```

### แจกแต้ม
```
🎁 กำลังแจก 50 แต้มให้ทุกคน...
✅ สมชาย ใจดี: 100 → 150 แต้ม
✅ สมหญิง รักงาน: 250 → 300 แต้ม
...
📊 สรุปผลการแจกแต้ม:
   ✅ สำเร็จ: 85 คน
   💰 แต้มรวมทั้งหมดที่แจก: 4,250 แต้ม
```

---

## 🆘 Troubleshooting

### ปัญหา: Script รันไม่ได้
```bash
# ตรวจสอบ Node.js version
node --version  # ต้อง >= 18

# ติดตั้ง dependencies
npm install
```

### ปัญหา: ไม่เห็น Notification
1. ตรวจสอบ `notifications` collection ใน Firestore
2. เช็ค Console เว็บ (F12)
3. ลบ `seenNotifications` subcollection ของ user นั้น

### ปัญหา: แต้มไม่เพิ่ม
1. เช็ค `activityLogs` collection
2. ดูข้อผิดพลาดใน script output
3. ตรวจสอบ Firestore Rules

---

## 📝 หมายเหตุ

- ✅ Scripts ทั้งหมดใช้ Firebase Admin SDK
- ✅ ต้องมีไฟล์ `egoke-7dae5-091db05d83c0.json` ใน `functions/`
- ✅ Notification จะโชว์ครั้งเดียว ไม่รบกวนผู้ใช้
- ✅ Migration ปลอดภัย ไม่ลบข้อมูลเดิม

---

## 🎉 สำเร็จ!

หลังรันทั้ง 3 สคริป:
1. ✅ ตั๋วเก่าทั้งหมดถูก migrate
2. ✅ ทุกคนได้ 50 แต้ม
3. ✅ Notification พร้อมโชว์
4. ✅ ระบบ QR Scan เร็วขึ้น (ไม่ต้อง fallback)

**ขอบคุณผู้เข้าร่วมงานทุกท่าน! 🙏✨**
