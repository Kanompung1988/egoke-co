# 👑 Super Admin System - คู่มือการใช้งาน

## 📌 ภาพรวม

ระบบ Super Admin ช่วยให้คุณจัดการผู้ใช้ทั้งหมดในระบบ EGOKE ได้ รวมถึง:
- ✅ เปลี่ยน Role ของผู้ใช้ (User → Staff → Admin)
- ✅ ดูสถิติผู้ใช้แบบเรียลไทม์
- ✅ ค้นหาผู้ใช้ด้วย Email, ชื่อ, หรือ Role
- ✅ จัดการสิทธิ์อย่างปลอดภัยผ่าน Firestore Security Rules

---

## 🚀 วิธีเริ่มต้นใช้งาน (สำหรับครั้งแรก)

### ขั้นตอนที่ 1: Deploy Firestore Security Rules

**สำคัญมาก!** ต้องทำก่อนใช้งาน SuperAdmin

#### ตัวเลือก A: ใช้ Script อัตโนมัติ (แนะนำ)
```bash
cd /Users/t333838/Downloads/EGOKE
chmod +x setup-superadmin.sh
./setup-superadmin.sh
```

#### ตัวเลือก B: Deploy ด้วยมือ
```bash
# 1. Login เข้า Firebase
firebase login

# 2. ตั้งโปรเจกต์
firebase use egoke-7dae5

# 3. Deploy Rules
firebase deploy --only firestore:rules
```

#### ตัวเลือก C: Copy-Paste ผ่าน Firebase Console
1. เปิด: https://console.firebase.google.com/project/egoke-7dae5/firestore/rules
2. คัดลอกโค้ดจาก `firestore.rules`
3. Paste ลงใน Console
4. คลิก "Publish"

---

### ขั้นตอนที่ 2: Login ด้วย SuperAdmin Email

1. เปิดเว็บไซต์: https://egoke.areazeroai.com หรือ http://localhost:5173
2. คลิก "Login with Google"
3. เลือกบัญชี: **thanaponchanal@gmail.com**
4. หลัง Login สำเร็จ ระบบจะตั้ง Role เป็น `superadmin` อัตโนมัติ

---

### ขั้นตอนที่ 3: เข้าสู่หน้า Super Admin

1. หลัง Login แล้ว ไปที่: `/superadmin` หรือคลิกปุ่ม "Super Admin" (ถ้ามี)
2. คุณจะเห็นหน้าจัดการผู้ใช้ทั้งหมด

---

## 🎯 ฟีเจอร์หลัก

### 1. 📊 Dashboard สถิติ
แสดงจำนวนผู้ใช้แบ่งตาม Role:
- 👑 SuperAdmin (1 คน - ปกป้อง)
- 🔧 Admin (สามารถจัดการ Votes, Prizes)
- 👷 Staff (สามารถสแกน QR, เพิ่ม Points)
- 👤 User (ผู้ใช้ทั่วไป)

### 2. 🔍 ค้นหาผู้ใช้
สามารถค้นหาด้วย:
- อีเมล (example@gmail.com)
- ชื่อผู้ใช้ (Display Name)
- Role (user, staff, admin)

### 3. ⚡ เปลี่ยน Role แบบ Quick Action
ในตาราง มีปุ่ม:
- 👷 = ตั้งเป็น Staff
- 🔧 = ตั้งเป็น Admin
- 👤 = ลดกลับเป็น User

คลิกปุ่มเดียว ระบบจะอัปเดททันที!

### 4. 🛡️ ป้องกัน SuperAdmin
- SuperAdmin (thanaponchanal@gmail.com) จะมีป้าย "🔒 ปกป้อง"
- ห้ามมีใครเปลี่ยน Role ของ SuperAdmin ได้
- SuperAdmin เท่านั้นที่เปลี่ยน Role ของคนอื่นได้

---

## 👥 ประเภท Role และสิทธิ์

| Role | สิทธิ์ | Icon |
|------|--------|------|
| **SuperAdmin** | จัดการ Role ของทุกคน, ดูข้อมูลทั้งหมด | 👑 |
| **Admin** | จัดการ Votes, Prizes, ดูสถิติ | 🔧 |
| **Staff** | สแกน QR Code, เพิ่ม Points ให้ User | 👷 |
| **User** | โหวต, เล่นเกม, ดูข้อมูลตัวเอง | 👤 |

---

## 📸 ตัวอย่างการใช้งาน

### เปลี่ยน User ให้เป็น Staff

**วิธีที่ 1: ใช้ Dropdown (สำหรับการแก้ไขหลายคนพร้อมกัน)**
1. เลือก Email จาก Dropdown ด้านบน
2. เลือก Role ที่ต้องการ (Staff)
3. คลิก "✅ อัพเดทสิทธิ์"
4. จะเห็นข้อความสีเขียว "✅ อัพเดท ... เป็น staff สำเร็จ!"

**วิธีที่ 2: ใช้ Quick Action ในตาราง (เร็วกว่า!)**
1. หา User ที่ต้องการในตาราง
2. คลิกปุ่ม 👷 (Staff) ในคอลัมน์ "จัดการ"
3. ระบบจะอัปเดททันที!

---

## 🔒 Firestore Security Rules

### กฎความปลอดภัยที่ใช้:

```javascript
// SuperAdmin เท่านั้นที่แก้ไข Role ได้
function isSuperAdmin() {
  return request.auth.token.email == 'thanaponchanal@gmail.com';
}

// User ทั่วไปห้ามแก้ role, points, tickets
allow update: if isAuthenticated() && (
  (isOwner(userId) && 
   !request.resource.data.diff(resource.data).affectedKeys().hasAny(['role', 'points', 'tickets']))
  || isSuperAdmin()
);
```

### ความปลอดภัย:
- ✅ User ทั่วไปห้ามเปลี่ยน Role ของตัวเอง
- ✅ User ทั่วไปห้ามเพิ่ม Points ให้ตัวเอง
- ✅ เฉพาะ SuperAdmin เท่านั้นที่ลบ User ได้
- ✅ Staff/Admin อ่านข้อมูล User ได้ทั้งหมด

---

## 🛠️ แก้ไขปัญหา

### ปัญหา 1: ไม่สามารถเข้าหน้า SuperAdmin
**อาการ:** เห็นข้อความ "⛔ คุณไม่มีสิทธิ์เข้าถึงหน้านี้"

**สาเหตุ:**
- Login ด้วยอีเมลที่ไม่ใช่ `thanaponchanal@gmail.com`
- Role ใน Firestore ยังไม่ได้เป็น `superadmin`

**วิธีแก้:**
1. Logout ออกจากระบบ
2. Login ใหม่ด้วย `thanaponchanal@gmail.com`
3. ระบบจะตั้ง Role เป็น `superadmin` อัตโนมัติ

---

### ปัญหา 2: กดเปลี่ยน Role แล้วไม่เปลี่ยน
**อาการ:** กดปุ่มแล้วไม่มีอะไรเกิดขึ้น หรือเห็น Error

**สาเหตุ:**
- Firestore Rules ยังไม่ได้ Deploy
- Firebase Rules บล็อคการเขียนข้อมูล

**วิธีแก้:**
1. Deploy Firestore Rules:
   ```bash
   firebase deploy --only firestore:rules
   ```
2. รีเฟรชหน้าเว็บ (F5)
3. ลองใหม่อีกครั้ง

---

### ปัญหา 3: ไม่เห็นรายชื่อ User
**อาการ:** ตารางว่างเปล่า หรือโหลดไม่เสร็จ

**สาเหตุ:**
- Firestore Rules บล็อคการอ่านข้อมูล
- ยังไม่มี User คนอื่นใน Database

**วิธีแก้:**
1. ตรวจสอบ Firebase Console: https://console.firebase.google.com/project/egoke-7dae5/firestore
2. Deploy Rules ใหม่:
   ```bash
   firebase deploy --only firestore:rules
   ```
3. เปิด Developer Console (F12) ดู Error

---

### ปัญหา 4: PERMISSION_DENIED Error
**อาการ:** เห็น Error ใน Console: `FirebaseError: Missing or insufficient permissions`

**สาเหตุ:** Firestore Rules ไม่อนุญาต

**วิธีแก้:**
1. ตรวจสอบว่า Login ด้วย SuperAdmin Email: `thanaponchanal@gmail.com`
2. Deploy Rules:
   ```bash
   firebase deploy --only firestore:rules
   ```
3. Logout และ Login ใหม่

---

## 📁 ไฟล์ที่เกี่ยวข้อง

```
/Users/t333838/Downloads/EGOKE/
├── firestore.rules                 # Firestore Security Rules
├── firestore.indexes.json          # Firestore Indexes
├── firebase.json                   # Firebase Config (อัปเดตแล้ว)
├── setup-superadmin.sh             # Script Deploy อัตโนมัติ
├── FIRESTORE_RULES_SETUP.md        # คู่มือ Deploy Rules
├── SUPERADMIN_GUIDE.md             # คู่มือนี้
└── src/
    ├── firebaseApp.ts              # Firebase Functions
    └── page/
        └── SuperAdmin.tsx          # หน้า SuperAdmin (อัปเดตแล้ว)
```

---

## 🎯 เช็คลิสต์ก่อนใช้งาน

- [ ] ✅ Deploy Firestore Rules สำเร็จ
- [ ] ✅ Login ด้วย `thanaponchanal@gmail.com`
- [ ] ✅ Role ใน Firestore เป็น `superadmin`
- [ ] ✅ เข้าหน้า `/superadmin` ได้
- [ ] ✅ เห็นรายชื่อ User ในตาราง
- [ ] ✅ สามารถเปลี่ยน Role ได้

---

## 📞 ต้องการความช่วยเหลือเพิ่มเติม?

1. อ่าน: `FIRESTORE_RULES_SETUP.md`
2. เปิด Developer Console (F12) ดู Error
3. ตรวจสอบ Firestore Console: https://console.firebase.google.com/project/egoke-7dae5/firestore

---

## 🎉 สำเร็จแล้ว!

ตอนนี้คุณสามารถ:
- 👑 จัดการผู้ใช้ทั้งหมดใน EGOKE
- 🛡️ ตั้ง Staff/Admin ได้อย่างปลอดภัย
- 📊 ดูสถิติผู้ใช้แบบเรียลไทม์
- 🔍 ค้นหาและกรอง User ได้ง่าย

**ขอให้โชคดีกับการจัดการระบบ!** 🚀
