# 🔥 วิธีเปิดใช้งาน Firebase Storage

## ⚠️ ปัญหา
**Admin ไม่สามารถเพิ่มผู้สมัครได้เมื่ออัพโหลดรูป**

**Error**: `storage/unauthorized` หรือ `permission-denied`

**สาเหตุ**: Firebase Storage ยังไม่ได้เปิดใช้งานในโปรเจค

---

## ✅ วิธีแก้ไข

### ขั้นตอนที่ 1: เปิดใช้งาน Firebase Storage

1. เข้า **Firebase Console**:
   ```
   https://console.firebase.google.com/project/egoke-7dae5/storage
   ```

2. คลิก **"Get Started"** หรือ **"เริ่มต้นใช้งาน"**

3. เลือก **"Start in production mode"** (จะตั้งค่า rules ภายหลัง)

4. เลือก **Location**:
   - แนะนำ: `asia-southeast1 (Singapore)` 
   - หรือ `asia-east1 (Taiwan)`
   - เลือกให้ใกล้ผู้ใช้ที่สุด

5. คลิก **"Done"**

6. **รอ 2-3 นาที** ให้ Firebase เปิด Storage API

---

### ขั้นตอนที่ 2: Deploy Storage Rules

หลังจากเปิด Storage แล้ว รอ **2-3 นาที** แล้วรันคำสั่ง:

```bash
firebase deploy --only storage
```

**Expected Output:**
```
✔  storage: released rules storage.rules to firebase.storage
✔  Deploy complete!
```

---

### ขั้นตอนที่ 3: ทดสอบการอัพโหลด

1. **เข้าหน้า Admin Dashboard**:
   ```
   https://egoke.co/admin
   ```

2. **คลิก "เพิ่มผู้สมัคร"**

3. **อัพโหลดรูปภาพ**:
   - กรอกชื่อผู้สมัคร
   - เลือกไฟล์รูป (ไม่เกิน 5MB)
   - คลิก "เพิ่มผู้สมัคร"

4. **ตรวจสอบผล**:
   - ✅ ถ้าสำเร็จ: จะเห็นข้อความ "✅ เพิ่มผู้สมัครสำเร็จ"
   - ❌ ถ้าผิดพลาด: ดูข้อความ error และแก้ตามคำแนะนำ

---

## 🔍 ตรวจสอบว่า Storage เปิดแล้วหรือยัง

### วิธีที่ 1: เช็คใน Firebase Console
```
https://console.firebase.google.com/project/egoke-7dae5/storage
```
- ถ้าเห็นหน้า Storage Dashboard (มี bucket) = เปิดแล้ว ✅
- ถ้าเห็น "Get Started" button = ยังไม่เปิด ❌

### วิธีที่ 2: เช็คใน Terminal
```bash
firebase deploy --only storage
```
- ถ้าขึ้น `✔ storage: released rules` = เปิดแล้ว ✅
- ถ้าขึ้น error `Storage API not enabled` = ยังไม่เปิด ❌

---

## 🛠️ แก้ปัญหาเพิ่มเติม

### ปัญหา 1: `storage-quota-exceeded`
**สาเหตุ**: พื้นที่ Storage เต็ม

**วิธีแก้**:
- ลบไฟล์เก่าที่ไม่ใช้แล้ว
- หรืออัพเกรด Firebase plan

### ปัญหา 2: `storage-unauthenticated`
**สาเหตุ**: ไม่ได้ล็อกอิน

**วิธีแก้**:
- Logout แล้ว Login ใหม่

### ปัญหา 3: `permission-denied` หลัง deploy rules
**สาเหตุ**: Rules ยังไม่มีผล (ต้องรอ propagation)

**วิธีแก้**:
- รอ 2-3 นาที
- Deploy rules อีกครั้ง:
  ```bash
  firebase deploy --only storage
  ```

---

## 📋 Storage Rules ที่ใช้

ไฟล์: `storage.rules`

```javascript
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    
    // Candidates images
    match /candidates/{category}/{fileName} {
      // Anyone can read candidate images
      allow read: if true;
      
      // Only admin/superadmin can upload/update/delete
      allow write: if isAdmin();
      
      // Limit file size to 5MB
      allow write: if request.resource.size < 5 * 1024 * 1024;
      
      // Only allow image files
      allow write: if request.resource.contentType.matches('image/.*');
    }
  }
}
```

---

## 🎯 สรุป

1. ✅ **เปิด Firebase Storage** ใน Console
2. ⏱️ **รอ 2-3 นาที**
3. 🚀 **Deploy rules**: `firebase deploy --only storage`
4. 🧪 **ทดสอบ**: อัพโหลดรูปในหน้า Admin

---

## 📞 ติดปัญหา?

ถ้ายังอัพโหลดไม่ได้ ให้เช็ค:
- [ ] Firebase Storage เปิดแล้วหรือยัง?
- [ ] รอ 2-3 นาที หลังเปิดหรือยัง?
- [ ] Deploy storage rules หรือยัง?
- [ ] ล็อกอินเป็น Admin/SuperAdmin หรือเปล่า?
- [ ] ไฟล์รูปเป็น image file และไม่เกิน 5MB หรือเปล่า?
