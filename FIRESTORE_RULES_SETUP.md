# 🔒 คู่มือการติดตั้ง Firestore Security Rules

## ⚠️ สำคัญมาก! ต้องทำก่อนใช้งาน SuperAdmin

ไฟล์ `firestore.rules` คือกฎความปลอดภัยของ Firebase Firestore ที่จะควบคุมว่า:
- **ใครบ้างที่สามารถอ่าน/เขียนข้อมูลได้**
- **SuperAdmin สามารถจัดการ users ได้**
- **User ธรรมดาห้ามแก้ role, points, tickets ของตัวเอง**

---

## 🚀 วิธีที่ 1: Deploy ผ่าน Firebase CLI (แนะนำ)

### ขั้นตอน:

1. **ติดตั้ง Firebase CLI (ถ้ายังไม่มี)**
   ```bash
   npm install -g firebase-tools
   ```

2. **Login เข้า Firebase**
   ```bash
   firebase login
   ```

3. **ตรวจสอบว่าโปรเจกต์ถูกต้อง**
   ```bash
   firebase use egoke-7dae5
   ```

4. **Deploy Firestore Rules**
   ```bash
   firebase deploy --only firestore:rules
   ```

5. **รอสักครู่ แล้วจะเห็นข้อความ**
   ```
   ✔  Deploy complete!
   ```

---

## 🖥️ วิธีที่ 2: อัปโหลดผ่าน Firebase Console (ทำด้วยมือ)

### ขั้นตอน:

1. **เปิด Firebase Console**
   - ไปที่: https://console.firebase.google.com/project/egoke-7dae5/firestore/rules

2. **คัดลอกโค้ดจากไฟล์ `firestore.rules`**
   - เปิดไฟล์ `/Users/t333838/Downloads/EGOKE/firestore.rules`
   - Copy โค้ดทั้งหมด

3. **วางโค้ดใน Firebase Console**
   - ลบโค้ดเก่าทิ้ง
   - Paste โค้ดใหม่ที่คัดลอกมา

4. **คลิก "Publish" (เผยแพร่)**
   - รอสักครู่จะเห็นข้อความสีเขียว "Rules published successfully"

---

## ✅ ตรวจสอบว่า Rules ทำงาน

หลังจาก Deploy แล้ว ให้ทดสอบ:

1. **Login ด้วย thanaponchanal@gmail.com**
2. **เข้าหน้า `/superadmin`**
3. **ลองเปลี่ยน role ของ user คนอื่น**

ถ้าทำได้ แสดงว่า Rules ทำงานถูกต้อง! ✨

---

## 📋 สิ่งที่ Firestore Rules นี้ทำ

### 🔐 Users Collection (`/users/{userId}`)
- ✅ **SuperAdmin (`thanaponchanal@gmail.com`)** → อ่าน/เขียน/ลบได้ทุกอย่าง
- ✅ **Admin/Staff** → อ่านได้ทุก user
- ✅ **User ทั่วไป** → อ่าน/แก้ไขได้เฉพาะตัวเอง (แต่ห้ามแก้ `role`, `points`, `tickets`)

### 🗳️ Votes & VoteCategories
- ✅ **อ่านได้:** ทุกคนที่ login แล้ว
- ✅ **เขียน/แก้ไข/ลบ:** เฉพาะ Admin/Staff

### 🎰 Prizes & SpinHistory
- ✅ **อ่านได้:** ทุกคนที่ login แล้ว (SpinHistory เห็นเฉพาะของตัวเอง)
- ✅ **เขียน/แก้ไข/ลบ:** เฉพาะ Admin/Staff

### 📱 QRScans
- ✅ **อ่าน/เขียน:** เฉพาะ Admin/Staff

### ⚙️ Settings
- ✅ **อ่านได้:** ทุกคนที่ login แล้ว
- ✅ **เขียน/แก้ไข/ลบ:** เฉพาะ Admin/Staff

---

## 🛡️ ความปลอดภัยที่เพิ่มขึ้น

| ก่อน Deploy Rules | หลัง Deploy Rules |
|---|---|
| ❌ User ทั่วไปแก้ไข points ของตัวเองได้ | ✅ ห้ามแก้ - ต้องใช้ Admin เท่านั้น |
| ❌ User ทั่วไปแก้ไข role ของตัวเองเป็น admin ได้ | ✅ ห้ามแก้ - ต้องใช้ SuperAdmin |
| ❌ User ทั่วไปอ่านข้อมูลทุก user ได้ | ✅ อ่านได้เฉพาะตัวเอง |
| ❌ ใครก็ได้สามารถลบ document | ✅ ลบได้เฉพาะ SuperAdmin |

---

## 🚨 แก้ไขปัญหาที่พบบ่อย

### ปัญหา 1: `PERMISSION_DENIED` เมื่อโหลดข้อมูล
**สาเหตุ:** Rules ยังไม่ถูก Deploy  
**วิธีแก้:** Deploy rules ด้วยคำสั่ง `firebase deploy --only firestore:rules`

### ปัญหา 2: SuperAdmin จัดการ user ไม่ได้
**สาเหตุ:** อีเมลไม่ตรง `thanaponchanal@gmail.com`  
**วิธีแก้:** 
1. ตรวจสอบว่า login ด้วยอีเมลที่ถูกต้อง
2. Logout แล้ว Login ใหม่

### ปัญหา 3: User ทั่วไปยังแก้ไข role ตัวเองได้
**สาเหตุ:** Rules ยังไม่ทำงาน หรือ Deploy ไม่สำเร็จ  
**วิธีแก้:** Deploy rules อีกครั้ง และรีเฟรชหน้าเว็บ

---

## 📞 ต้องการความช่วยเหลือ?

ถ้ายังติดปัญหา ให้:
1. ✅ Deploy rules ตามขั้นตอนข้างบน
2. ✅ Logout และ Login ใหม่ด้วย `thanaponchanal@gmail.com`
3. ✅ เปิด Developer Console (F12) ดูว่ามี error อะไร

---

**🎉 เสร็จแล้ว! ระบบ SuperAdmin พร้อมใช้งาน!**
