# 🧪 วิธีทดสอบระบบ SuperAdmin

## 📋 เช็คลิสต์การทดสอบ

### ✅ ขั้นตอนที่ 1: เตรียมระบบ

#### 1.1 Deploy Firestore Rules (สำคัญที่สุด!)
```bash
cd /Users/t333838/Downloads/EGOKE
./setup-superadmin.sh
```

หรือถ้าไม่ได้ ให้ใช้:
```bash
firebase login
firebase use egoke-7dae5
firebase deploy --only firestore:rules
```

**ผลลัพธ์ที่คาดหวัง:**
```
✔  Deploy complete!

Project Console: https://console.firebase.google.com/project/egoke-7dae5/overview
```

#### 1.2 ตรวจสอบว่า Rules ถูก Deploy แล้ว
1. เปิด: https://console.firebase.google.com/project/egoke-7dae5/firestore/rules
2. คุณควรเห็นโค้ดที่มี:
   ```javascript
   function isSuperAdmin() {
     return request.auth.token.email == 'thanaponchanal@gmail.com';
   }
   ```

---

### ✅ ขั้นตอนที่ 2: รัน Dev Server

```bash
cd /Users/t333838/Downloads/EGOKE
npm run dev
```

**ผลลัพธ์ที่คาดหวัง:**
```
VITE v5.x.x  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

---

### ✅ ขั้นตอนที่ 3: Login ด้วย SuperAdmin

1. **เปิดเบราว์เซอร์:** http://localhost:5173
2. **คลิก "Login with Google"**
3. **เลือกบัญชี:** `thanaponchanal@gmail.com`
4. **รอ Login สำเร็จ**

**ผลลัพธ์ที่คาดหวัง:**
- เห็นหน้า Home
- มีข้อมูลผู้ใช้ (ชื่อ, อีเมล, Points)
- ใน Developer Console (F12) เห็น:
  ```
  ✅ Google auth success: thanaponchanal@gmail.com
  ✨ New user created with role: superadmin
  หรือ
  👑 SuperAdmin updated
  ```

---

### ✅ ขั้นตอนที่ 4: เข้าหน้า SuperAdmin

1. **ไปที่:** http://localhost:5173/superadmin
2. **รอโหลด**

**ผลลัพธ์ที่คาดหวัง:**
- เห็นหน้า Super Admin Panel
- มี Header: "👑 Super Admin Panel"
- มี Statistics Cards (👥 ทั้งหมด, 👑 SuperAdmin, ฯลฯ)
- มีช่องค้นหา
- มีตารางแสดงรายชื่อผู้ใช้

**ถ้าเจอปัญหา:**
- หน้าเปลี่ยนไปที่ "/" → อ่านขั้นตอนที่ 7
- โหลดไม่เสร็จ → ดู Developer Console (F12) มี error อะไร

---

### ✅ ขั้นตอนที่ 5: ทดสอบดูรายชื่อ User

1. **ดูในตาราง** ควรเห็น:
   - Email
   - ชื่อผู้ใช้
   - Role (พร้อม Badge สี)
   - Points
   - ปุ่มจัดการ

**ผลลัพธ์ที่คาดหวัง:**
```
Email                    ชื่อ           Role              Points      จัดการ
thanaponchanal@gmail.com Admin Thanapon [👑 SuperAdmin]   999,999,999  🔒 ปกป้อง
user1@gmail.com          User 1         [👤 User]         100         [👷][🔧]
user2@gmail.com          User 2         [👷 Staff]        500         [👤][🔧]
```

**ถ้าไม่เห็นรายชื่อ:**
- เปิด Developer Console (F12)
- ดู Console tab มี error อะไร?
- ดู Network tab มี 403 (Forbidden) หรือไม่?
- → อ่านขั้นตอนที่ 7

---

### ✅ ขั้นตอนที่ 6: ทดสอบเปลี่ยน Role

#### วิธีที่ 1: ใช้ Quick Action (ในตาราง)
1. **หา User ที่ไม่ใช่ SuperAdmin** (เช่น user1@gmail.com)
2. **คลิกปุ่ม 👷** (Staff) ในคอลัมน์ "จัดการ"
3. **รอสักครู่**

**ผลลัพธ์ที่คาดหวัง:**
- เห็นข้อความสีเขียว: "✅ อัพเดท user1@gmail.com เป็น staff สำเร็จ!"
- Badge เปลี่ยนเป็น [👷 Staff]
- ข้อความหายไปหลัง 3 วินาที

#### วิธีที่ 2: ใช้ Dropdown
1. **เลือก Email** จาก Dropdown ด้านบน
2. **เลือก Role** ที่ต้องการ (เช่น Staff)
3. **คลิก "✅ อัพเดทสิทธิ์"**

**ผลลัพธ์ที่คาดหวัง:**
- เห็นข้อความสีเขียว
- ตารางอัปเดทอัตโนมัติ

---

### ✅ ขั้นตอนที่ 7: แก้ไขปัญหาที่พบ

#### ปัญหา A: "⛔ คุณไม่มีสิทธิ์เข้าถึงหน้านี้"
```
สาเหตุ: Role ใน Firestore ยังไม่เป็น superadmin

แก้ไข:
1. Logout ออก
2. Login ใหม่ด้วย thanaponchanal@gmail.com
3. ถ้ายังไม่ได้:
   - เปิด Firebase Console: https://console.firebase.google.com/project/egoke-7dae5/firestore/data/~2Fusers
   - หา Document ที่มี email = thanaponchanal@gmail.com
   - แก้ field "role" เป็น "superadmin"
   - บันทึก
   - Logout และ Login ใหม่
```

#### ปัญหา B: ไม่เห็นรายชื่อ User (ตารางว่าง)
```
สาเหตุ: Firestore Rules บล็อค

แก้ไข:
1. Deploy Rules:
   firebase deploy --only firestore:rules

2. เปิด Developer Console (F12)
   - Console tab มี error อะไร?
   - มี "FirebaseError: Missing or insufficient permissions" หรือไม่?

3. ตรวจสอบ Firebase Console:
   https://console.firebase.google.com/project/egoke-7dae5/firestore/rules
   - Rules ถูกต้องหรือไม่?
   - มี function isSuperAdmin() หรือไม่?

4. Logout และ Login ใหม่
```

#### ปัญหา C: กดปุ่มแล้วไม่เปลี่ยน Role
```
สาเหตุ: Firestore Rules ยังไม่ Deploy

แก้ไข:
1. Deploy Rules:
   firebase deploy --only firestore:rules

2. รีเฟรชหน้าเว็บ (F5)

3. ลองใหม่

4. ถ้ายังไม่ได้:
   - เปิด Developer Console (F12)
   - คลิกปุ่มอีกครั้ง
   - ดู Console tab มี error อะไร?
   - ถ่ายภาพ error ส่งมาดู
```

#### ปัญหา D: "PERMISSION_DENIED" ใน Console
```
Error: FirebaseError: Missing or insufficient permissions.

สาเหตุ: Firestore Rules ไม่ถูกต้อง หรือยังไม่ Deploy

แก้ไข:
1. ตรวจสอบว่า Login ด้วย thanaponchanal@gmail.com
   - ใช่หรือไม่? ถ้าไม่ใช่ → Logout และ Login ใหม่

2. Deploy Rules:
   firebase deploy --only firestore:rules

3. ตรวจสอบใน Firebase Console:
   https://console.firebase.google.com/project/egoke-7dae5/firestore/rules
   - คัดลอกโค้ดจาก firestore.rules
   - Paste ลงไป
   - คลิก "Publish"

4. Logout และ Login ใหม่
```

---

### ✅ ขั้นตอนที่ 8: ทดสอบระบบค้นหา

1. **พิมพ์ในช่องค้นหา:** "user1"
2. **ดูผลลัพธ์:** ควรเห็นเฉพาะ user ที่มี "user1" ใน email หรือชื่อ

**ผลลัพธ์ที่คาดหวัง:**
- ตารางกรองเหลือเฉพาะที่ค้นหา
- จำนวนในหัวตารางเปลี่ยน (เช่น "รายชื่อผู้ใช้ทั้งหมด (1 คน)")

---

### ✅ ขั้นตอนที่ 9: ทดสอบ Statistics

**ผลลัพธ์ที่คาดหวัง:**
- การ์ด Statistics อัปเดทตามจำนวนจริง
- เมื่อเปลี่ยน Role สถิติจะเปลี่ยนทันที

ตัวอย่าง:
```
ก่อน:  👤 5 User, 👷 2 Staff
(เปลี่ยน 1 User เป็น Staff)
หลัง: 👤 4 User, 👷 3 Staff
```

---

## 🎯 เช็คลิสต์สุดท้าย

- [ ] ✅ Deploy Firestore Rules สำเร็จ
- [ ] ✅ Login ด้วย thanaponchanal@gmail.com ได้
- [ ] ✅ เข้าหน้า /superadmin ได้
- [ ] ✅ เห็นรายชื่อ User ในตาราง
- [ ] ✅ เปลี่ยน Role ได้ (ด้วย Quick Action)
- [ ] ✅ เปลี่ยน Role ได้ (ด้วย Dropdown)
- [ ] ✅ ค้นหา User ได้
- [ ] ✅ Statistics แสดงถูกต้อง
- [ ] ✅ SuperAdmin มีป้าย "🔒 ปกป้อง"

---

## 📹 ขั้นตอนทดสอบแบบเต็ม (Copy-Paste ได้เลย)

```bash
# 1. Deploy Rules
cd /Users/t333838/Downloads/EGOKE
firebase login
firebase use egoke-7dae5
firebase deploy --only firestore:rules

# 2. รัน Dev Server
npm run dev

# 3. เปิดเบราว์เซอร์
# http://localhost:5173

# 4. Login ด้วย thanaponchanal@gmail.com

# 5. ไปที่ /superadmin

# 6. ทดสอบเปลี่ยน Role

# เสร็จ! 🎉
```

---

## 🆘 ต้องการความช่วยเหลือ?

**ถ้ายังติดปัญหา:**
1. ถ่ายภาพหน้าจอ
2. เปิด Developer Console (F12) ถ่ายภาพ Console tab
3. บอกว่าทำถึงขั้นตอนไหนแล้ว

**ไฟล์ที่เกี่ยวข้อง:**
- `firestore.rules` - Security Rules
- `src/page/SuperAdmin.tsx` - หน้า SuperAdmin
- `src/firebaseApp.ts` - Firebase Functions
- `FIRESTORE_RULES_SETUP.md` - คู่มือ Deploy
- `SUPERADMIN_GUIDE.md` - คู่มือใช้งาน
- `SUMMARY.md` - สรุปการแก้ไข

---

**🎊 ขอให้การทดสอบราบรื่น!**
